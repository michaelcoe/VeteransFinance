<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.interam.veteransfinance" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SecondScreen"
            android:label="GI Bill Info"
            android:theme="@style/AppTheme" />

        <activity android:name=".ThirdScreen"
            android:label="Semester Info"
            android:theme="@style/AppTheme" />

        <activity android:name=".FourthScreen"
            android:label="More Semester Info"
            android:theme="@style/AppTheme" />

        <activity android:name=".FifthScreen"
            android:label="Benefit Calculations"
            android:theme="@style/AppTheme" />

    </application>

</manifest>
